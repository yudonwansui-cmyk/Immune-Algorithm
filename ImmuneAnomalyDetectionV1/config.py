# config.py (适配新版 '诊断' 数据集)

"""
配置文件 - 适配新版威斯康星乳腺癌（诊断）数据集
"""

# ==================================
# 1. 数据集相关参数
# ==================================
# 数据集的文件路径，确保你的CSV文件名为 'data.csv' 并放在 `data` 文件夹下
DATA_PATH = "data/data.csv"

# 数据集的特征列名。根据你的截图，这个数据集有30个特征。
# 我们把它们都列出来。第一列 'id' 和最后一列 'Unnamed: 32' 是无用的。
# 'diagnosis' 是我们的标签列。
# 为了方便，我们直接从数据中提取，这里可以先留空或写一部分。
# 在data_processor.py中我们会智能地处理它。

# 标签所在的列名
LABEL_COLUMN = 'diagnosis'

# 定义标签值：'B' 代表良性 (Benign)，我们视其为“正常”
NORMAL_LABEL_VALUE = 'B'
# 定义标签值：'M' 代表恶性 (Malignant)，我们视其为“异常”
ANOMALY_LABEL_VALUE = 'M'


# ==================================
# 2. 数据处理相关参数
# ==================================
TEST_SIZE = 0.3
RANDOM_STATE = 42


# ==================================
# 3. 人工免疫算法核心参数
# ==================================
# 这部分保持不变，是你进行实验和调优的核心
DETECTOR_RADIUS = 2.25  # 对于这个高维数据，半径可能需要适当调大一点来开始实验
NUM_DETECTORS_TO_GENERATE = 1000 # 特征多了，空间变大，检测器数量也可以多一些
MAX_GENERATION_TRIES = 100000