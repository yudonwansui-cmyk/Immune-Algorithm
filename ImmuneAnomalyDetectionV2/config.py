# config.py (V3 - 适配信用卡欺诈检测数据集)

# ==================================
# 1. 数据集相关参数
# ==================================
DATA_PATH = "data/creditcard.csv"

# 标签所在的列名
LABEL_COLUMN = 'Class'

# 定义标签值：0 代表正常交易
NORMAL_LABEL_VALUE = 0
# 定义标签值：1 代表欺诈交易
ANOMALY_LABEL_VALUE = 1

# ==================================
# 2. 数据处理相关参数
# ==================================
# !!! 新增：数据采样配置 !!!
# 由于数据集很大，为了快速进行参数搜索，我们可以先在数据的子集上进行实验。
# 设置为 True 以启用采样，设置为 False 则使用全部数据。
USE_SAMPLING = False
# 正常样本的采样比例 (例如，只取10%的正常交易)
NORMAL_SAMPLE_FRAC = 0.1
# 异常样本的采样比例 (通常我们希望保留所有稀有的异常样本)
ANOMALY_SAMPLE_FRAC = 1.0

# 测试集比例和随机种子保持不变
TEST_SIZE = 0.3
RANDOM_STATE = 42

# ==================================
# 3. 人工免疫算法核心参数
# ==================================
# !!! 注意：最优参数需要重新寻找 !!!
# 之前为乳腺癌数据集找到的参数在这里完全不适用。
# 我们需要从一个全新的、更适合这个数据集的起点开始。
# 这个数据集特征维度高(30维)，且数据点非常密集。
# 通常，半径 r 需要设置得更小。
DETECTOR_RADIUS = 0.01  # 这是一个全新的起始猜测值，需要通过tuner.py重新寻找
NUM_DETECTORS_TO_GENERATE = 2000 # 起始数量
MAX_GENERATION_TRIES = 1000000 # 可以适当增大